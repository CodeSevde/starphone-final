<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>İlanı Düzenle | İlan Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        .form-card { border-radius: 20px; border: none; }
    </style>
</head>
<body class="bg-light">

   <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold fs-3" href="/"><i class="bi bi-shop me-2 text-warning"></i>İLAN PORTAL</a>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav ms-auto align-items-center">
                <a class="nav-link px-3" href="#"><i class="bi bi-person-circle me-1"></i> Giriş Yap</a>
                <span class="text-secondary mx-1">|</span>
                <a class="nav-link px-3" href="#">Üye Ol</a>
                <a class="btn btn-warning ms-lg-3 fw-bold rounded-3 px-4 shadow-sm" href="/ekle">
                    <i class="bi bi-plus-lg me-1"></i> Ücretsiz İlan Ver
                </a>
            </div>
        </div>
    </div>
</nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-7">
                <div class="card shadow-sm form-card p-4 p-md-5 border-top border-warning border-5">
                    <div class="text-center mb-4">
                        <i class="bi bi-pencil-square text-warning display-4"></i>
                        <h2 class="fw-bold mt-2">İlanı Güncelle</h2>
                        <p class="text-muted">İlan ID: #<%= ilan.id %></p>
                    </div>

                    <form action="/duzenle/<%= ilan.id %>" method="POST">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Başlık</label>
                            <input type="text" name="baslik" class="form-control form-control-lg" value="<%= ilan.baslik %>" required>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Kategori</label>
                                <select name="kategori" class="form-select">
                                    <option value="Emlak" <%= ilan.kategori === 'Emlak' ? 'selected' : '' %>>Emlak</option>
                                    <option value="Vasıta" <%= ilan.kategori === 'Vasıta' ? 'selected' : '' %>>Vasıta</option>
                                    <option value="Elektronik" <%= ilan.kategori === 'Elektronik' ? 'selected' : '' %>>Elektronik</option>
                                    <option value="Diğer" <%= ilan.kategori === 'Diğer' ? 'selected' : '' %>>Diğer</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label fw-bold">Fiyat</label>
                                <input type="text" name="fiyat" class="form-control" value="<%= ilan.fiyat %>" required>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">Açıklama</label>
                            <textarea name="aciklama" class="form-control" rows="5" required><%= ilan.aciklama %></textarea>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning fw-bold p-3">Değişiklikleri Kaydet</button>
                            <a href="/" class="btn btn-link text-decoration-none text-muted">Değişiklik yapmadan geri dön</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>