<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StarPhone | Teknolojinin Parlayan Yıldızı</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <style>
        body {
            background-color: #f4f6f8;
        }

        /* NAVBAR */
        .navbar {
            background-color: #0f172a;
        }

        /* HERO */
        .hero-section {
            background: linear-gradient(45deg, #1a1a2e, #16213e);
            color: white;
            padding: 90px 0;
        }

        /* CARD */
        .ilan-card {
            border-radius: 20px;
            transition: all 0.3s ease;
            background: white;
        }
        .ilan-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 25px 45px rgba(0,0,0,0.15);
        }

        .price-tag {
            font-size: 1.3rem;
            font-weight: bold;
            color: #198754;
        }

        .badge-ios { background-color: #000; }
        .badge-android { background-color: #3DDC84; color: #000; }
        .badge-klasik { background-color: #6c757d; }

        .badge {
            padding: 6px 14px;
            border-radius: 999px;
            font-weight: 600;
        }
    </style>
</head>

<body>

<nav class="navbar navbar-dark sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold fs-3" href="/">
            <i class="bi bi-stars text-warning me-2"></i>STARPHONE
        </a>

        <div class="d-flex align-items-center text-white">
            <a class="nav-link px-3 small" href="/hakkimizda">Hakkımızda</a>

            <% if (user) { %>
                <span class="small me-3">
                    Hoş geldin, <strong><%= user.name %></strong>
                </span>

                <a href="/logout" class="btn btn-outline-light btn-sm rounded-pill">
                    Çıkış
                </a>

                <a class="btn btn-warning ms-3 fw-bold rounded-pill px-4" href="/ekle">
                    İlan Ver
                </a>
            <% } else { %>
                <form action="/login" method="POST" class="d-flex align-items-center ms-3">
                    <input type="text" name="name"
                           class="form-control form-control-sm me-2"
                           placeholder="Kullanıcı Adı" required>

                    <input type="password" name="password"
                           class="form-control form-control-sm me-2"
                           placeholder="Şifre" required>

                    <button class="btn btn-warning btn-sm fw-bold rounded-pill px-3">
                        Giriş
                    </button>
                </form>

                <a href="/register"
                   class="btn btn-outline-warning btn-sm rounded-pill px-3 ms-2">
                    Üye Ol
                </a>
            <% } %>
        </div>
    </div>
</nav>

<section class="hero-section text-center">
    <div class="container">
        <h1 class="display-5 fw-bold mb-4">
            Yeni Telefonunu Yıldızların Arasında Bul
        </h1>

        <form action="/" method="GET"
              class="input-group input-group-lg mx-auto"
              style="max-width: 900px;">
            <input type="text" name="search"
                   class="form-control"
                   placeholder="Model ara..."
                   value="<%= aramaKelimesi || '' %>">

            <select name="category" class="form-select" style="max-width: 150px;">
                <option value="">Tümü</option>
                <option value="iOS" <%= seciliKategori === 'iOS' ? 'selected' : '' %>>iOS</option>
                <option value="Android" <%= seciliKategori === 'Android' ? 'selected' : '' %>>Android</option>
                <option value="Klasik" <%= seciliKategori === 'Klasik' ? 'selected' : '' %>>Klasik</option>
            </select>

            <button class="btn btn-warning fw-bold px-4">ARA</button>
        </form>

        <% if (aramaYapildiMi) { %>
            <p class="mt-3 small opacity-75">
                <%= ilanlar.length %> sonuç bulundu ·
                <a href="/" class="text-warning">Temizle</a>
            </p>
        <% } %>
    </div>
</section>

<div class="container my-5">
    <div class="row">
        <% ilanlar.forEach(ilan => { %>
            <div class="col-lg-4 col-md-6 mb-4">
                <div class="card ilan-card h-100 shadow-sm border-0">
                    <img src="<%= ilan.resim %>"
                         class="card-img-top"
                         style="height:220px; object-fit:cover;">

                    <div class="card-body">
                        <span class="badge badge-<%= ilan.kategori.toLowerCase() %> mb-2">
                            <%= ilan.kategori %>
                        </span>

                        <h5 class="fw-bold"><%= ilan.baslik %></h5>
                        <div class="price-tag mb-3"><%= ilan.fiyat %></div>

                        <div class="d-flex justify-content-between align-items-center border-top pt-3">
                            <a href="/detay/<%= ilan.id %>"
                               class="btn btn-primary btn-sm rounded-pill px-4">
                                İncele
                            </a>

                            <% if (user && (user.role === 'admin' || ilan.ownerId === user.id)) { %>
                                <div class="d-flex gap-2">
                                    <a href="/duzenle/<%= ilan.id %>" class="text-secondary">
                                        <i class="bi bi-pencil-square fs-5"></i>
                                    </a>
                                    <a href="/sil/<%= ilan.id %>"
                                       onclick="return confirm('Bu ilanı silmek istediğine emin misin?')"
                                       class="text-danger">
                                        <i class="bi bi-trash3 fs-5"></i>
                                    </a>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

<footer class="bg-dark text-white text-center py-3">
    <small class="opacity-50">
        StarPhone © 2026 – Web Tasarımı Final Projesi
    </small>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
