<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StarPhone | Teknolojinin Parlayan Yıldızı</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <style>
        .ilan-card { 
            border-radius: 20px; 
            transition: all 0.3s ease; 
            background: white;
        }
        .ilan-card:hover { 
            transform: translateY(-8px); 
            box-shadow: 0 25px 45px rgba(0,0,0,0.15); 
        }

        .hero-section { 
            background: linear-gradient(45deg, #1a1a2e, #16213e); 
            color: white; 
            padding: 80px 0;
            position: relative;
            overflow: hidden;
        }

        /* 🔥 HERO DERİNLİK EFEKTİ (EKLEME) */
        .hero-section::after {
            content: "";
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at top, rgba(255,255,255,0.08), transparent 65%);
            pointer-events: none;
        }

        .price-tag { 
            font-size: 1.3rem; 
            font-weight: bold; 
            color: #198754; 
        }

        .badge-ios { background-color: #000; }
        .badge-android { background-color: #3DDC84; color: #000; }
        .badge-klasik { background-color: #6c757d; }

        /* 🔥 ROZET MODERNLEŞTİRME */
        .badge {
            padding: 6px 14px;
            border-radius: 999px;
            font-weight: 600;
            letter-spacing: .3px;
        }

        /* 🔥 INPUT ODAK EFEKTİ */
        input.form-control:focus,
        select.form-select:focus {
            box-shadow: 0 0 0 4px rgba(250, 204, 21, 0.25);
            border-color: #facc15;
        }

        /* 🔥 NAVBAR CAM EFEKTİ */
        .navbar {
            backdrop-filter: blur(6px);
        }
    </style>
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-dark sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold fs-3" href="/">
            <i class="bi bi-stars text-warning me-2"></i>STARPHONE
        </a>

        <div class="d-flex align-items-center text-white">
            <% if (user) { %>
                <span class="me-3 small">Hoş geldin, <strong><%= user.name %></strong></span>
                <a href="/logout" class="btn btn-outline-danger btn-sm rounded-pill">Çıkış</a>
            <% } else { %>
                <a class="nav-link px-2 small" onclick="showAuthModal('login')">Giriş</a>
                <span class="opacity-50 mx-1">|</span>
                <a class="nav-link px-2 small" onclick="showAuthModal('register')">Üye Ol</a>
                <span class="opacity-50 mx-1">|</span>
                <a class="nav-link px-2 small" href="/hakkimizda">Hakkımızda</a>
            <% } %>

            <a class="btn btn-warning ms-3 fw-bold rounded-pill px-4" href="/ekle">İlan Ver</a>
        </div>
    </div>
</nav>

<section class="hero-section text-center">
    <div class="container">
        <h1 class="display-5 fw-bold mb-4">Yeni Telefonunu Yıldızların Arasında Bul</h1>

        <form action="/" method="GET" class="input-group input-group-lg mx-auto" style="max-width: 900px;">
            <input type="text" name="search" class="form-control" placeholder="Model ara..."
                   value="<%= aramaKelimesi || '' %>">

            <select name="category" class="form-select" style="max-width: 150px;">
                <option value="">Tümü</option>
                <option value="iOS" <%= seciliKategori === 'iOS' ? 'selected' : '' %>>iOS</option>
                <option value="Android" <%= seciliKategori === 'Android' ? 'selected' : '' %>>Android</option>
                <option value="Klasik" <%= seciliKategori === 'Klasik' ? 'selected' : '' %>>Klasik</option>
            </select>

            <button class="btn btn-warning fw-bold px-4">ARA</button>
        </form>

        <% if (aramaYapildiMi) { %>
            <p class="mt-3 small opacity-75">
                <%= ilanlar.length %> sonuç bulundu · <a href="/" class="text-warning">Temizle</a>
            </p>
        <% } %>
    </div>
</section>

<div class="container my-5">
    <div class="row">
        <% if (ilanlar.length > 0) { %>
            <% ilanlar.forEach(ilan => { %>
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card ilan-card h-100 shadow-sm border-0">
                        <img src="<%= ilan.resim %>" class="card-img-top"
                             onerror="this.src='/images/android/samsung1.jpeg'"
                             style="height:220px; object-fit:cover;">

                        <div class="card-body">
                            <span class="badge badge-<%= ilan.kategori.toLowerCase() %> mb-2">
                                <%= ilan.kategori %>
                            </span>

                            <h5 class="fw-bold"><%= ilan.baslik %></h5>
                            <div class="price-tag mb-3"><%= ilan.fiyat %></div>

                            <div class="d-flex justify-content-between align-items-center border-top pt-3">
                                <a href="/detay/<%= ilan.id %>" class="btn btn-primary btn-sm rounded-pill px-4">
                                    İncele
                                </a>

                                <% if (user) { %>
                                    <a href="/sil/<%= ilan.id %>"
                                       onclick="return confirm('Bu ilan silinsin mi?')"
                                       class="text-danger">
                                        <i class="bi bi-trash3 fs-5"></i>
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="text-center py-5">
                <h4 class="text-muted">Sonuç bulunamadı</h4>
                <a href="/" class="btn btn-outline-primary btn-sm mt-2">Tüm İlanlar</a>
            </div>
        <% } %>
    </div>
</div>

<!-- AUTH MODAL -->
<div class="modal fade" id="authModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-4 p-4">
            <div id="loginFormSection">
                <h4 class="fw-bold mb-3 text-center">Giriş Yap</h4>
                <form action="/login" method="POST">
                    <input type="email" name="email" class="form-control mb-3" placeholder="E-posta" required>
                    <input type="password" name="password" class="form-control mb-3" placeholder="Şifre" required>
                    <button class="btn btn-primary w-100">Giriş</button>
                </form>
            </div>

            <div id="registerFormSection" style="display:none;">
                <h4 class="fw-bold mb-3 text-center">Üye Ol</h4>
                <form action="/register" method="POST">
                    <input type="text" name="name" class="form-control mb-3" placeholder="Ad Soyad" required>
                    <input type="email" name="email" class="form-control mb-3" placeholder="E-posta" required>
                    <input type="password" name="password" class="form-control mb-3" placeholder="Şifre" required>
                    <button class="btn btn-success w-100">Kayıt Ol</button>
                </form>
            </div>
        </div>
    </div>
</div>

<footer class="bg-dark text-white text-center py-3">
    <small class="opacity-50">StarPhone © 2026 – Web Tasarımı Final Projesi</small>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    const authModal = new bootstrap.Modal(document.getElementById('authModal'));
    function showAuthModal(type) {
        document.getElementById('loginFormSection').style.display = type === 'login' ? 'block' : 'none';
        document.getElementById('registerFormSection').style.display = type === 'register' ? 'block' : 'none';
        authModal.show();
    }
</script>

</body>
</html>
